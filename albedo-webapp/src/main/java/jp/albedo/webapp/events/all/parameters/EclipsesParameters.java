package jp.albedo.webapp.events.all.parameters;

public class EclipsesParameters {

    private final boolean enabled;

    public EclipsesParameters(boolean enabled) {
        this.enabled = enabled;
    }

    public boolean isEnabled() {
        return enabled;
    }
}
