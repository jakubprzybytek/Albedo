import { vi, describe, it, expect } from "vitest";
import { StateSolverBuilder } from './StateSolverBuilder';

describe("StateSolverBuilder", () => {

    it("should use DirectStateSolver when apprioprate", () => {
       
    });

});
